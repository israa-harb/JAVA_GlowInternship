Access modifiers help to restrict the scope of a class, constructor, variable, method, or data member. It provides security, accessibility to the user depending upon the access modifier used with the element. There are four types of access modifiers available in Java: Default, Private, Protected, Public.
1. Default Access Modifier: when no access modifier is specified for a class, method, or data member – It is said to be having the default access modifier by default. The data members, classes, or methods that are not declared using any access modifiers; having default access modifiers are accessible only within the same package.